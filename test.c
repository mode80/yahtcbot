// #include "ints.h"
#include <stdio.h> // printf

//(int*)mints->array,[10]

int main() {

    // ints* mints = ints_init(2) ; 
    // ints_set(mints,0, 1);
    // ints_set(mints,1, 2);
    // printf("mints[0] = %d, mints[1] = %d \n", ints_get(mints,0), ints_get(mints,1));

    // ints_recap(mints, 4);
    // ints_set(mints,2, 3);
    // printf("mints[0] = %d, mints[1] = %d, mints[2] = %d \n", ints_get(mints,0), ints_get(mints,1), ints_get(mints,2));

    // ints* mints2 = ints_new() ;
    // ints_recap(mints2, 4);
    // ints_set(mints2,0,42);

    // ints_append(mints, mints2);
    // printf("mints[0] = %d, mints[1] = %d, mints[2] = %d, mints[11] = %d \n", ints_get(mints,0), ints_get(mints,1), ints_get(mints,2), ints_get(mints,11));
    // size_t len = ints_count(mints);
    // assert(len == 8);

    // ints* mints3 = ints_from_array((int[]){1,2,3,4,5}, 5);

    // ints_destroy(mints);
    // ints_destroy(mints2);

}
