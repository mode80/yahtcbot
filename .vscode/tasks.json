{
    "tasks": [
        {
            "type": "cppbuild",
            "label": "Build Release",
            "command": "clang",
            "args": [
                "-O3",
                "-o",
                "${workspaceFolder}/yahtcbot",
                "${workspaceFolder}/yahtcbot.c",
              ],
            "options": { "cwd": "${fileDirname}" },
            // "problemMatcher": [ "$gcc" ],
            "group": { "kind": "build", "isDefault": true}
        } ,
        {
            "type": "cppbuild",
            "label": "Build Debug",
            "command": "clang",
            "args": ["-g", "-o", "${workspaceFolder}/yahtcbot", "${workspaceFolder}/*.c"],
            "options": { "cwd": "${fileDirname}" },
            // "problemMatcher": [ "$gcc" ],
            "group": { "kind": "build", "isDefault":false}
        }
    ],
    "version": "2.0.0"
}